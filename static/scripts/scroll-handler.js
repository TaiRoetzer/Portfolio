class ScrollHandler {
  constructor() {
    this.deltaY = 0;
    this.positionY = 0;
  }

  update({ positionY }) {
    if (this.positionY < 0)
  }
}

window.addEventListener('scroll', () => {
  const CONSTANTS = {
    
  };

  const { scrollY } = window;

  if (y >= )
})